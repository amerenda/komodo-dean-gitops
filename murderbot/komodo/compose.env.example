## Komodo Periphery (murderbot)
## Ansible writes the real compose.env from this template, injecting the
## periphery passkey from Bitwarden Secrets Manager. compose.env is gitignored.

## Image base tag (matches mac-mini-m4 Komodo Core).
COMPOSE_KOMODO_IMAGE_TAG=2.1.2

## Timezone
TZ=America/New_York

## Periphery
PERIPHERY_ROOT_DIRECTORY=/etc/komodo
PERIPHERY_DISABLE_TERMINALS=false
PERIPHERY_SSL_ENABLED=true
PERIPHERY_LOGGING_PRETTY=false
PERIPHERY_PRETTY_STARTUP_CONFIG=false

## Mirror the host's view of bind-mount roots so Periphery's "include disk"
## sanity checks succeed and Komodo Core sees host-real paths.
PERIPHERY_INCLUDE_DISK_MOUNTS=/etc/hostname,/mnt/storage,/mnt/storage/media/config

## Enable the periphery's inbound HTTP server on :8120.
## Core connects inward to check server health, pull container stats, and
## execute stack operations. Required: outbound mode (PERIPHERY_CORE_ADDRESSES)
## does not complete the v2.1.2 handshake successfully via k3s ingress.
PERIPHERY_SERVER_ENABLED=true

## Shared secret with Komodo Core (mac-mini-m4) — Ansible replaces this from
## Bitwarden secret komodo-dean-passkey before docker compose up.
PERIPHERY_PASSKEYS=ANSIBLE_WILL_REPLACE_THIS
