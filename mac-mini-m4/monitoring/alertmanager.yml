# Placeholder — inject-secrets.sh overwrites this file on disk with the real Pushover
# receiver (BWS: pushover-user-key, pushover-alerts-api-key) and reloads Alertmanager.
# See mac-mini-m4/scripts/inject-secrets.sh. Never put real credentials in this file.
global:
  resolve_timeout: 5m

route:
  receiver: 'no-op'
  group_by: ['alertname', 'cluster']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  routes: []

receivers:
  - name: 'no-op'
